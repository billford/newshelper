# Reference copy for lampoon:~/chatbot-newshelper/server/prod.env (ADR-003
# Phase 2). The real file (with the real secret) lives only on lampoon --
# never commit an actual INBOUND_API_KEY to this repo.

UPSTREAM_URL=http://localhost:40114/olla/ollama/v1/chat/completions
UPSTREAM_MODEL=llama3.1:8b
INBOUND_API_KEY=replace-with-a-fresh-value-from-openssl-rand-hex-32
ALLOWED_ORIGINS=https://billford.github.io
MAX_BODY_BYTES=32768
PORT=8789
RETRIEVAL_URL=http://192.168.1.100:8901/retrieve
RETRIEVAL_TIMEOUT_MS=5000
